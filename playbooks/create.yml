- name: Create Azure Kubernetes Cluster
  hosts: azure
  vars_files:
    - ../vars/defaults.yaml
    - ../vars/vault.yaml
  roles:
    # - role: setup_cluster
    # - role: pull_kube_admin_config
    # - role: setup_dns
    - role: deploy_ingress_controller